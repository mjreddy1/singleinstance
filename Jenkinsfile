pipeline {
    agent any
    
    stages {
        stage("Build") {
            steps {
                echo "Some code compilation here..."
            }
        }
        stage("Deploy") {
            steps {
                echo "Some code compilation here..."
            }
        }
        stage("Test") {
            steps {
                echo "Some tests compilation here..."
                echo "1"
            }
        }
        
    }
}
